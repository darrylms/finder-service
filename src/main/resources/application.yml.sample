micronaut:
  router:
    static-resources:
      swagger:
        paths: classpath:META-INF/swagger
        mapping: /swagger/**
  security:
    enabled: true
    endpoints:
      login:
        enabled: true
  application:
    name: finder-service

auth:
  username:
  password:
connections:
  retry: 3
finder-transport-service:
  app-id:
  app-key:
google-maps-service:
  api-key:
